import numpy as np
import matplotlib.pyplot as plt


def compute_2D_string(X, Y, V, begin, end, transitions=None, npts=100):
    """
    Computes the minimum free energy path connecting the points `begin`
    and `end`. Points passed through `transitions` are used to generate
    an initial guess (which interpolates through all the points).

    Args:
        X:
        Y:
        V:
        begin:
        end:
        transitions:
        npts:
    """
    pass
